services:
  db:
    image: postgres:16-alpine
    container_name: hotflix-db
    environment:
      POSTGRES_DB: hotflix
      POSTGRES_USER: hotflix
      POSTGRES_PASSWORD: hotflix
    ports:
      - "5432:5432"
    volumes:
      - hotflix-data:/var/lib/postgresql/data

volumes:
  hotflix-data:
